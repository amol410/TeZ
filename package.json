{
  "name": "tezsend",
  "version": "1.0.0",
  "description": "TezSend - Fintech App",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd web && npm install --include=dev && npm run build",
    "postinstall": "npm --prefix backend install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
